# vim: set ft=conkyrc:
conky.config = {
    short_units = true,
    background = true,
    out_to_console = true,
    out_to_x = false,
    update_interval = 3
}

conky.text = [[
S ${execi 1 ./panel_volume} | ${execi 1 ./panel_battery} | ${execi 1 ./panel_clock}
]]
